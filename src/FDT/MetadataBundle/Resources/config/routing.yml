FDTMetadataBundle_homepage:
    pattern:  /hello/{name}
    defaults: { _controller: FDTMetadataBundle:Default:index }

FDTMetadataBundle_getLanguages:
    pattern:  /admin/{bundleName}/getLanguages
    defaults: { _controller: FDTMetadataBundle:getLanguages:index}

FDTMetadataBundle_getAttributiTipo:
    pattern:  /admin/{bundleName}/getAttributiTipo
    defaults: { _controller: FDTMetadataBundle:getAttributiTipo:index}

FDTMetadataBundle_manageTipologie:
    pattern:  /admin/{bundleName}/{tipologia}/manageTipologie/{id}
    defaults: { _controller: contenuti.manageTipologie:execute, id: false}

FDTMetadataBundle_manageAttributi:
    pattern:  /admin/{bundleName}/manageAttributi/{id}
    defaults: { _controller: contenuti.manageAttributi:indexAction, id: false}

FDTMetadataBundle_manageDataset:
    pattern:  /admin/{bundleName}/manageDataset/{id}
    defaults: { _controller: contenuti.manageDataset:indexAction, id: false}

FDTMetadataBundle_manageOptions:
    pattern:  /admin/{bundleName}/manageOptions/{id}
    defaults: { _controller: contenuti.manageOptions:indexAction, id: false}

FDTMetadataBundle_manageRelations:
    pattern:  /admin/{bundleName}/manageRelations/{id}
    defaults: { _controller: contenuti.manageRelations:indexAction, id: false}

FDTMetadataBundle_manageSearch:
    pattern:  /admin/{bundleName}/manageSearch/{id}
    defaults: { _controller: contenuti.manageSearch:indexAction, id: false}